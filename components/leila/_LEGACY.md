# LEGACY — do not actively develop

The active chat implementation lives at `/langchain` (`components/langchain/`).

This directory (`components/leila/`) is the original Leila prototype built on
`CopilotChat` from `@copilotkit/react-ui`. It is kept for reference only.

Do not apply UI improvements here — apply them to the langchain components instead.
